<%= form_tag (topic_search_path, :method => 'get')  do %>
<p> <%= text_field_tag :search, params[:search] %>
<%= submit_tag "Search", :name => nil %></p>
<% end %>
<%= link_to 'View all',topics_path%> 
<h1><%= pluralize(@topics.length,'Result')  %> </h1>

<%= render :partial => 'topics/topic_light', :collection => @topics, :as => :topic %>
